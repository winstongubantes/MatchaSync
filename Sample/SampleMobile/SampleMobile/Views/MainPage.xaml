<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="SampleMobile.Views.MainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:converters="clr-namespace:SampleMobile.Converters;assembly=SampleMobile"
    Title="Mobile Sync Samples">

    <StackLayout Padding="24" VerticalOptions="Center">
        <Button Command="{Binding ShowFullSyncSampleCommand}" Text="Todo List (OData)" />
        <Button Command="{Binding ShowStatusSyncSampleCommand}" Text="Todo-Complete (OData)" />
        <Button Command="{Binding ShowFullSyncWebApiSampleCommand}" Text="Todo List (WebApi)" />
        <Button Command="{Binding ShowODataCallCommand}" Text="OData Function Call" />
        <Button Command="{Binding ShowWebApiCallCommand}" Text="WebApi Method Call" />
        <Button Command="{Binding ShowPaginationPageCommand}" Text="Pagination" />
    </StackLayout>
</ContentPage>